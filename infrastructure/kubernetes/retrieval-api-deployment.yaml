apiVersion: apps/v1
kind: Deployment
metadata:
  name: retrieval-api
  labels: { app: retrieval-api }
spec:
  replicas: 2
  selector:
    matchLabels: { app: retrieval-api }
  template:
    metadata:
      labels: { app: retrieval-api }
    spec:
      containers:
        - name: retrieval-api
          image: spotify-memory/retrieval-api:latest
          ports: [{ containerPort: 8002 }]
          envFrom:
            - secretRef: { name: memory-system-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8002 }
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8002 }
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: retrieval-api
spec:
  selector: { app: retrieval-api }
  ports:
    - port: 8002
      targetPort: 8002
